# Node Express Demo

## Setup Tasks
- Create web pages
- Server file
- setup a node project
- create a git repo
- create gitignore to hide securite stuff and packages from npm

## Create a web server
- hello world example (make sure the basics work)
- serve html pages
- serve css & js
- server images

## Setup Node Project
- `npm init -y` (sets up a node project)
- `npm run serve` (runs our application)
- Check package.json file (what is it?)
